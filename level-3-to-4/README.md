# Bandit Level 3 → Level 4

**Goal:** The password for the next level is stored in a hidden file in the `inhere` directory.

---

### 🧰 Commands used

```bash
ls
cd inhere
ls -la
cat "...Hiding-From-You"
# -> PASSWORD_REDACTED


💡 What I learned
How to find hidden files using ls -la. (Files starting with a . are hidden).

The importance of typing filenames exactly as they appear.
